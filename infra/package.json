{
  "name": "mhe-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure for the mHealthy Hearts backend (VPC, RDS, ECS Fargate, ALB, CloudFront)",
  "bin": {
    "mhe-infra": "bin/mhe-infra.ts"
  },
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy"
  },
  "devDependencies": {
    "aws-cdk": "^2.0.0",
    "cdk-nag": "^3.0.2",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.0.0",
    "constructs": "^10.0.0"
  }
}
